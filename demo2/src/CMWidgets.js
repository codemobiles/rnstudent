import React from 'react';
import {
  View,
  Text,
  ImageBackground,
  Image,
  TextInput,
  TouchableOpacity,
  Button,
} from 'react-native';

const Spacer = (props) => {
  return <View style={{width: props.size}} />;
};

export {Spacer};
